{
  "name": "neatpix",
  "description": "neatpix staging deploy",
  "version": "1.0.0",
  "engines": {
    "npm": "2.1.x"
  },
  "scripts": {
    "preinstall": "npm install --save bower",
    "postinstall": "./node_modules/.bin/bower install"
  },
  "dependencies": {
    "bower": "^1.7.1"
  },
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-sass": "^2.1.0",
    "gulp-autoprefixer": "^3.1.0"
  }
}
